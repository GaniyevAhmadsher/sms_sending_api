# Scaling

- Scale API by CPU and p95 latency.
- Scale worker by queue depth and oldest job age.
- Redis: use persistence + replica + sentinel.
- Postgres: tune autovacuum, indexes, and add read replica when needed.
