openapi: 3.0.3
info:
  title: SMS Sending SaaS API
  version: 1.0.0
paths:
  /auth/login:
    post:
      summary: Login
  /auth/refresh:
    post:
      summary: Exchange refresh token for new access token
  /onboarding/api-keys:
    post:
      summary: Create API key for tenant workspace
  /onboarding/webhook-callbacks:
    post:
      summary: Register customer callback URL and secret
  /analytics/daily-usage:
    get:
      summary: Daily usage trend for last 30 days
  /payments/webhook/click:
    post:
      summary: Click webhook receiver with replay protection headers
