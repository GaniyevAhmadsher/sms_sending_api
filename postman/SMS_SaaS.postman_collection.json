{
  "info": {
    "name": "SMS SaaS API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {"name": "Register", "request": {"method": "POST", "url": "{{baseUrl}}/auth/register"}},
    {"name": "Login", "request": {"method": "POST", "url": "{{baseUrl}}/auth/login"}},
    {"name": "Create API Key", "request": {"method": "POST", "url": "{{baseUrl}}/api-keys"}},
    {"name": "Send SMS", "request": {"method": "POST", "url": "{{baseUrl}}/sms/send"}}
  ]
}
